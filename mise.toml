[settings]
disable_tools = ["node"]

[tools]

# used for neovim lsp tests
"ubi:EmmyLuaLs/emmylua-analyzer-rust" = { version = "0.13.0", exe = "emmylua_ls", matching_regex = "^emmylua_ls" }

# small sample application that can be installed with mise and used in the test
# environment
cowsay = "latest"
