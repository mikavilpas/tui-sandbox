[settings]
disable_tools = ["node"]

[tools]
"github:EmmyLuaLs/emmylua-analyzer-rust" = { version = "0.24.0", matching = "emmylua_ls" }
"github:rvben/rumdl" = "0.2.33"

# small sample application that can be installed with mise and used in the test
# environment
cowsay = "latest"
zizmor = "1.27.0"
